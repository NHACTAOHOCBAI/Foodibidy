import { Tabs } from 'expo-router'
import { StyleSheet, Text, View } from 'react-native'

const _layout = () => {
    return (
        <Tabs
            screenOptions={{
                headerShown: false,
            }}>
        </Tabs>
    )
}

export default _layout
